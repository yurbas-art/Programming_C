#ifndef CALC_H
#define CALC_H

#include <stdarg.h>

// Функция суммы
int sum(int count, ...);

// Функция максимума
int max(int count, ...);

// Функция минимума
int min(int count, ...);

// Функция среднего арифметического
float avg(int count, ...);

#endif
